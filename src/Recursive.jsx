import React, { Component } from "react";
import PropTypes from "prop-types";

export default class Recursive extends Component {
	static propTypes = {};

	render = () => <div>React Recursive</div>;
}
